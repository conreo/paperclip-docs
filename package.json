{
  "name": "paperclip-docs",
  "version": "0.2.1",
  "type": "module",
  "description": "Serve a local offline documentation corpus to Paperclip agents as governed, read-only tools: search, read, browse, and report the snapshot's age.",
  "scripts": {
    "build": "node ./esbuild.config.mjs",
    "dev": "node ./esbuild.config.mjs --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config ./vitest.config.ts",
    "test:watch": "vitest --config ./vitest.config.ts",
    "verify": "npm run typecheck && npm run build && npm test",
    "prepack": "npm run clean && node ./esbuild.config.mjs --no-sourcemap",
    "prepublishOnly": "npm run verify"
  },
  "paperclipPlugin": {
    "manifest": "./dist/manifest.js",
    "worker": "./dist/worker.js",
    "ui": "./dist/ui/"
  },
  "keywords": [
    "paperclip",
    "paperclip-plugin",
    "paperclipai",
    "documentation",
    "docs",
    "offline-docs",
    "okf",
    "open-knowledge-format",
    "knowledge-base",
    "docs-search",
    "ai-agents"
  ],
  "author": "conreo",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/conreo/paperclip-docs.git"
  },
  "homepage": "https://github.com/conreo/paperclip-docs#readme",
  "bugs": {
    "url": "https://github.com/conreo/paperclip-docs/issues"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "peerDependencies": {
    "@paperclipai/plugin-sdk": ">=2026.817.0",
    "react": ">=18"
  },
  "peerDependenciesMeta": {
    "@paperclipai/plugin-sdk": {
      "optional": true
    },
    "react": {
      "optional": true
    }
  },
  "devDependencies": {
    "@paperclipai/plugin-sdk": "^2026.817.0",
    "@types/node": "^24.6.0",
    "@types/react": "^19.0.8",
    "esbuild": "^0.27.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
